<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<OfficeApp
  xmlns="http://schemas.microsoft.com/office/appforoffice/1.1"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns:bt="http://schemas.microsoft.com/office/officeappbasictypes/1.0"
  xmlns:ov="http://schemas.microsoft.com/office/taskpaneappversionoverrides"
  xsi:type="TaskPaneApp">

  <!-- ── Identity (LOCAL dev copy — distinct GUID from the hosted manifest) ── -->
  <Id>6e5e6895-efe4-4d23-81fb-9b1d183a1fac</Id>
  <Version>1.0.0.0</Version>
  <ProviderName>ExcelCsvMapper</ProviderName>
  <DefaultLocale>en-US</DefaultLocale>
  <DisplayName DefaultValue="Excel → CSV Mapper (Local)" />
  <Description DefaultValue="Map Excel data to CSV templates and export — local dev build" />

  <!-- ── Icons (served by the local dev server) ── -->
  <IconUrl DefaultValue="http://localhost:3000/assets/icon-32.png" />
  <HighResolutionIconUrl DefaultValue="http://localhost:3000/assets/icon-64.png" />
  <SupportUrl DefaultValue="https://github.com/Musomoka/sage-stock-upload-/issues" />

  <!-- ── Host ── -->
  <Hosts>
    <Host Name="Workbook" />
  </Hosts>

  <!-- ── Default Settings ── -->
  <DefaultSettings>
    <SourceLocation DefaultValue="http://localhost:3000/index.html" />
  </DefaultSettings>

  <!-- ── Permissions ── -->
  <Permissions>ReadWriteDocument</Permissions>

  <!-- ── Version Overrides ── -->
  <VersionOverrides xmlns="http://schemas.microsoft.com/office/taskpaneappversionoverrides" xsi:type="VersionOverridesV1_0">

    <Hosts>
      <Host xsi:type="Workbook">
        <DesktopFormFactor>
          <GetStarted>
            <Title resid="GetStarted.Title"/>
            <Description resid="GetStarted.Description"/>
            <LearnMoreUrl resid="GetStarted.LearnMoreUrl"/>
          </GetStarted>

          <!-- ── Ribbon Tab ── -->
          <ExtensionPoint xsi:type="PrimaryCommandSurface">
            <OfficeTab id="TabHome">
              <Group id="ExcelCsvMapper.Group">
                <Label resid="Group.Label" />
                <Icon>
                  <bt:Image size="16" resid="Icon.16x16" />
                  <bt:Image size="32" resid="Icon.32x32" />
                  <bt:Image size="80" resid="Icon.80x80" />
                </Icon>

                <Control xsi:type="Button" id="ExcelCsvMapper.ShowTaskpane">
                  <Label resid="ShowTaskpane.Label" />
                  <Supertip>
                    <Title resid="ShowTaskpane.Title" />
                    <Description resid="ShowTaskpane.Description" />
                  </Supertip>
                  <Icon>
                    <bt:Image size="16" resid="Icon.16x16" />
                    <bt:Image size="32" resid="Icon.32x32" />
                    <bt:Image size="80" resid="Icon.80x80" />
                  </Icon>
                  <Action xsi:type="ShowTaskpane">
                    <TaskpaneId>ExcelCsvMapperPane</TaskpaneId>
                    <SourceLocation resid="Taskpane.Url" />
                  </Action>
                </Control>
              </Group>
            </OfficeTab>
          </ExtensionPoint>
        </DesktopFormFactor>
      </Host>
    </Hosts>

    <Resources>
      <bt:Images>
        <bt:Image id="Icon.16x16" DefaultValue="http://localhost:3000/assets/icon-16.png" />
        <bt:Image id="Icon.32x32" DefaultValue="http://localhost:3000/assets/icon-32.png" />
        <bt:Image id="Icon.80x80" DefaultValue="http://localhost:3000/assets/icon-80.png" />
      </bt:Images>

      <bt:Urls>
        <bt:Url id="Taskpane.Url" DefaultValue="http://localhost:3000/index.html" />
        <bt:Url id="GetStarted.LearnMoreUrl" DefaultValue="https://github.com/Musomoka/sage-stock-upload-" />
      </bt:Urls>

      <bt:ShortStrings>
        <bt:String id="Group.Label" DefaultValue="CSV Mapper" />
        <bt:String id="ShowTaskpane.Label" DefaultValue="Map &amp; Export (Local)" />
        <bt:String id="ShowTaskpane.Title" DefaultValue="ExcelCsvMapper" />
        <bt:String id="GetStarted.Title" DefaultValue="ExcelCsvMapper" />
      </bt:ShortStrings>

      <bt:LongStrings>
        <bt:String id="ShowTaskpane.Description" DefaultValue="Select an Excel range, map columns, and export a CSV matching your layout. (Local dev build)" />
        <bt:String id="GetStarted.Description" DefaultValue="Select a worksheet range, map columns to a CSV layout, and export. (Local dev build)" />
      </bt:LongStrings>
    </Resources>
  </VersionOverrides>
</OfficeApp>
